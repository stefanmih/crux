package com.crux;

import com.crux.cli.CommandLine;

/** Entry point launching the simple CLI. */
public class Main {
    public static void main(String[] args) {
        new CommandLine().run();
    }
}

